sudo: required

language: rvm

java:
  - 1.8

services:
  - docker

before_install:
  - docker pull saltstack/ubuntu-14.04
  - pip install -r requirements.txt
  - sudo useradd -Us /bin/bash nobody

script: bundle exec kitchen verify